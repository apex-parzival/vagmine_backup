version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir -p deploy_dist
        - cp -R * deploy_dist/ 2>/dev/null || true
  artifacts:
    baseDirectory: deploy_dist
    files:
      - '**/*'
  cache:
    paths: []
